syntax = "proto3";
package althea.microtx.v1;

option go_package = "github.com/althea-net/althea-L1/x/microtx/types";

// Params struct
message Params { uint64 microtx_fee_basis_points = 1; }

message GenesisState { Params params = 1; }